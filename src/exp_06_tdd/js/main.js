this.COMPANY = this.COMPANY || {};

this.COMPANY.Product = (function () {
    'use strict';

    /**
     * Person
     *
     * Erstellt eine Person
     * @constructor
     */
    function Product(a) {
        'use strict';

        if(a === void 0) {
            throw new Error();
        }
    }

    return Product;
}());